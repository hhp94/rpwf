# use_description()
# dep_imp = c("DBI", "parsnip", "hardhat", "recipes", "dplyr", "rlang", "R6",
#             "glue", "jsonlite", "purrr", "dials", "tidyr", "stats", "withr",
#             "RSQLite", "arrow", "dbplyr")
# for(i in dep_imp){
#  use_package(i, type = "Imports", min_version = NULL)
# }
# suggest_imp = c("dm", "testthat", "here")
# for(i in suggest_imp){
#  use_package(i, type = "Suggests", min_version = NULL)
# }
#
# library(devtools)
# library(testthat)
#
# usethis::use_readme_rmd()
# devtools::check()
# styler::style_pkg()
# usethis::use_coverage
